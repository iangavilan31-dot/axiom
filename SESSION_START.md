# AXIOM — session boot file
*Read this first after any /clear. Keep it current.*

- **What this is:** the math universe on a blackboard — flyable chalk web of all math. Full spec + goal-lock: `BRIEF.md` (the GOAL wins over everything).
- **Run:** `npm run dev` → http://localhost:5127 (strict port, registered in PROJECTS.md). Build: `npm run build` → `dist/` opens from disk.
- **Vault page:** `ObsidianPKM\entities\axiom.md`

## Status (2026-07-07)
- Scaffolded (Vite + TS + canvas 2D), port 5127 claimed, git initialized, brief compiled. Build starting.

## Next
- Core chalk engine (renderer/camera/layout) → data authoring (~450 nodes, 14 branches) → interactions → 5 signature features → juice → QA (3 blind critics ≥85, real clicks, visible Playwright page) → offline build.

## Decisions so far
- Blackboard universe look (Ian-picked; banned list still applies — no neon cyan/scanlines/mono microtype)
- Self-mark mastery, problems never gate (boss-gating declined)
- Full-skeleton-first scope
